# Laboratory ladder

Evidence before promotion. No stage is skipped by marketing.

| Stage | Gate |
|-------|------|
| **LAB** (current) | Single reference node · wire + threat drafts · single-process benchmarks · honest non-claims |
| **Adversarial lab** | Multi-host gossip · partition/loss/restart chaos · economic invariant CI · resource-proof MVP |
| **Public testnet** | Frozen wire subset · published adversarial evidence · external join path · still not mainnet |
| **Mainnet** | Explicit decision after sustained evidence — **not scheduled by this document** |

## Track priorities (lab)

1. **Multi-host gossip** — real peers beyond single-process simulation  
2. **Resource proof MVP** — claimed capacity ≠ accepted contribution  
3. **Economic invariants I1–I6 in CI**  
4. **KYC attestation-only path** — no raw identity documents on ledger  
5. Application surfaces only after the above harden

Detail and schemas: `stratamesh-core` docs and `src/protocol_benchmark.py`.
